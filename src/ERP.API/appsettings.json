{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/erp-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 14
        }
      }
    ],
    "Enrich": [ "FromLogContext" ]
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=ERP;User Id=sa;Password=Your_strong_Passw0rd;TrustServerCertificate=True;",
    "Redis": "localhost:6379"
  },
  "Jwt": {
    "Issuer": "ERP.API",
    "Audience": "ERP.Client",
    "SigningKey": "dev-only-signing-key-change-me-in-production-min-32-bytes",
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 7
  },
  "//Seed": "Bootstrap administrator credentials are deliberately absent here. appsettings.json loads in EVERY environment, so a password in this file is a known-credential administrator on any public deployment. Development values live in appsettings.Development.json; elsewhere supply Seed__AdminEmail and Seed__AdminPassword as environment secrets.",
  "//Smtp": "Host is the switch: if Smtp:Host is empty, email falls back to the logging stub (see ERP.Infrastructure.DependencyInjection.AddNotifications). Smtp:Password is a secret and is deliberately absent here — supply it as the Smtp__Password environment variable in any deployed environment.",
  "Smtp": {
    "Host": "",
    "Port": 587,
    "User": "",
    "FromAddress": "",
    "FromName": "Nautilus ERP",
    "UseStartTls": true
  },
  "App": {
    "ClientBaseUrl": "http://localhost:5173"
  },
  "//Storage": "LocalPath is relative to the API content root and is only durable on a persistent disk. On ephemeral/multi-instance hosts (most PaaS platforms), replace LocalFileStorage with a cloud IFileStorage implementation instead of relying on this path.",
  "Storage": {
    "LocalPath": "App_Data/uploads"
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173", "http://localhost:3000" ]
  },
  "RateLimiting": {
    "AuthPerMinute": 20,
    "GeneralPerMinute": 200
  },
  "Sod": {
    "Enabled": true,
    "DisabledRules": []
  },
  "//AllowedHosts": "Override with your deployment hostname (AllowedHosts env var) so the app rejects host-header spoofing.",
  "AllowedHosts": "*",
  "//Sentry": "Dsn is the switch: leave it blank (the default) and the SDK stays disabled — safe for local dev and any fork without a Sentry project. Get a Dsn from sentry.io -> Settings -> Projects -> <project> -> Client Keys, and supply it via the Sentry__Dsn environment variable in any deployed environment. TracesSampleRate is 0 (no performance tracing, error capture only) until there's a reason to pay for the extra volume.",
  "Sentry": {
    "Dsn": "",
    "TracesSampleRate": 0.0,
    "SendDefaultPii": false
  }
}
